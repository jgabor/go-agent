# Minimal App Example

Run the smallest local app with no network calls or API keys:

```bash
go run ./examples/minimal
```

The example wires `New`, `NewTool`, a tiny local model, and an `EventSink`.
It stays runnable without network access or API keys.
